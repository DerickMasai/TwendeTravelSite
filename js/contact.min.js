var main = document.getElementById("main");var contactBtn = document.getElementById("contact-submit");var closeBtn = document.getElementById("close-btn");var confirmationDialog = document.getElementById("confirmation");var contactName = document.getElementById("contact-name");var contactEmail = document.getElementById("contact-email");var contactNumber = document.getElementById("contact-number");var contactMessage = document.getElementById("contact-message");var message = document.getElementById("message");var pMessage = document.getElementById("p-message");var messageCloseBtn = document.getElementById("message-close-btn");contactBtn.onclick = function () {contactNumber.value = contactNumber.value.replace(/ /g, "");var isNumberValid = "false";if (isNaN(contactNumber.value)) {isNumberValid = "false";} else {isNumberValid = "true";}if (!contactName.value.trim() || !contactEmail.value.trim() || !contactEmail.value.includes("@") || !contactNumber.value.trim() || isNumberValid == "false" || contactNumber.value.length < 10 || contactNumber.value.length > 13 || !contactMessage.value.trim()) {if (!contactName.value.trim()) {pMessage.innerHTML = "Heads up! You left out your <span>Name</span>";message.classList.add("message-active");setTimeout(function () {message.classList.remove("message-active");}, 3000);} else if (!contactEmail.value.trim()) {pMessage.innerHTML = "We'll also need your <span>Email Address</span>";message.classList.add("message-active");setTimeout(function () {message.classList.remove("message-active");}, 3000);} else if (!contactEmail.value.includes("@")) {pMessage.innerHTML = "We'll need a <span>valid</span> Email Address";message.classList.add("message-active");setTimeout(function () {message.classList.remove("message-active");}, 3000);} else if (!contactNumber.value.trim()) {pMessage.innerHTML = "Don't forget to add your <span>Phone Number</span>";message.classList.add("message-active");setTimeout(function () {message.classList.remove("message-active");}, 3000);} else if (isNumberValid == "false" || contactNumber.value.length < 10 || contactNumber.value.length > 13) {pMessage.innerHTML = "We'll need a <span>valid</span> Phone Number";message.classList.add("message-active");setTimeout(function () {message.classList.remove("message-active");}, 3000);} else if (!contactMessage.value.trim()) {pMessage.innerHTML = "We do need a <span>Message.</span> That's why you're here, right?";message.classList.add("message-active");setTimeout(function () {message.classList.remove("message-active");}, 3000);}} else {confirmationDialog.classList.add("confirmation-active");setTimeout(function () {confirmationDialog.classList.remove("confirmation-active")}, 5000);}}closeBtn.onclick = function () {confirmationDialog.classList.add("confirmation-drop");confirmationDialog.classList.remove("confirmation-active");}messageCloseBtn.onclick = function () {message.classList.remove("message-active");}